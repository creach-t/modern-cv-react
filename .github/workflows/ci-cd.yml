name: 🚀 CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# ═══════════════════════════════════════════════════════════
jobs:

  # ───────────────────────────────────────────────────────
  # JOB 1 — Tests & vérification du build
  # ───────────────────────────────────────────────────────
  test:
    name: 🧪 Tests & Build Check
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧪 Run tests
        run: npm test -- --watchAll=false --passWithNoTests --ci

      - name: 🏗️ Build verification
        run: npm run build

  # ───────────────────────────────────────────────────────
  # JOB 2 — Déploiement Zero-Downtime sur le serveur
  #         (uniquement sur push vers main)
  # ───────────────────────────────────────────────────────
  deploy:
    name: 🌐 Deploy (Zero Downtime)
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    environment:
      name: production
      url: https://creachtheo.fr

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📤 Upload deploy script sur le serveur
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          source: "scripts/deploy.sh"
          target: "~/deploy/"
          strip_components: 1

      - name: 🚀 Lancer le déploiement blue-green
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            chmod +x ~/deploy/deploy.sh
            ~/deploy/deploy.sh

      - name: ✅ Vérification post-déploiement
        run: |
          echo "⏳ Attente de la disponibilité du site..."
          sleep 10
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://creachtheo.fr/health)
          echo "HTTP status: $STATUS"
          if [ "$STATUS" != "200" ]; then
            echo "❌ Le site ne répond pas correctement (HTTP $STATUS)"
            exit 1
          fi
          echo "✅ Le site est opérationnel !"
